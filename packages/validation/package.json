{
  "name": "@smart-campus/validation",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "react-native": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo lint validation",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
